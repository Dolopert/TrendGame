# UPDATE — สถานะและบันทึกการตัดสินใจ

เอกสารส่งต่อสำหรับ session ถัดไป · อัปเดตล่าสุด **5 ก.ย. 2026**

`README.md` บอก *วิธีใช้* · `CONTEXT.md` บอก *คำศัพท์* · ไฟล์นี้บอก **สถานะปัจจุบัน
เหตุผลเบื้องหลังการตัดสินใจ และกับดักที่เคยเสียเวลาไปแล้ว** อ่านไฟล์นี้ก่อนแก้อะไร

---

## 1. โปรเจกต์นี้มีไว้ทำอะไร

เจ้าของเปิดร้านปล่อยเช่าไอดีเกม Steam ชื่อ **Online101Gaming** บนแพลตฟอร์ม
499k-network และ **เพิ่งเข้าตลาด** (ถือ 6 ไอดี จากทั้งตลาด 290 = 2.1%)

เครื่องมือนี้ตอบคำถามเดียว: **"ควรเอาเงินไปซื้อไอดีเกมไหนก่อนที่ร้านอื่นจะซื้อ"**

ไม่ใช่ "เกมไหนกำลังดัง" — เกมดังส่วนใหญ่เป็น F2P ปล่อยเช่าไม่ได้ และเกมที่คู่แข่ง
สต็อกไว้ 50 ไอดีแล้วก็ไม่ใช่สนามของรายใหม่

---

## 2. สถานะระบบ ณ ตอนนี้

### อัตโนมัติทั้งหมดแล้ว ไม่ต้องสั่งอะไรรายวัน

| เวลา (ไทย) | รันที่ไหน | ทำอะไร | ต้องเปิดคอม | ตรงเวลาไหม |
|---|---|---|---|---|
| 03:00 · 09:00 · 15:00 · 21:00 | GitHub Actions | Steam → dashboard → push | ไม่ต้อง | **หน่วง 43 นาที – 3.5 ชม.** |
| 22:00 | เครื่องบ้าน (Task Scheduler) | ตลาดเช่า **+ Steam** → dashboard → push | ต้อง (ปลุกจาก sleep ได้ถ้าเสียบปลั๊ก) | ตรงทุกครั้ง |

> **แก้บันทึกเดิม: cron ของ GitHub *รัน* แล้ว แต่หน่วงหนัก**
>
> ตอนตรวจเมื่อ 28 ส.ค. 22:00 ได้ `event=schedule` = **0 ครั้ง** จึงบันทึกไว้ว่า
> "cron ไม่รันจริง" — ซึ่งจริง ณ เวลานั้น แต่ **ตกลงมันเริ่มทำงานเองในอีกไม่กี่ชั่วโมง**
> ตรวจซ้ำ 30 ส.ค. ได้ 5 รอบ สำเร็จหมด น่าจะเป็นอาการ cron ของ repo ใหม่ที่ใช้เวลา
> ก่อนเริ่มทำงาน
>
> แต่หน่วงมาก เทียบกับ cron ที่ตั้งไว้ `0 2,8,14,20 UTC`:
> 14:00 → 17:30 (ช้า 3 ชม. 30) · 20:00 → 22:22 (ช้า 2 ชม. 22) · 02:00 → 02:43 (ช้า 43 นาที)
>
> **`scan` ที่เพิ่มเข้าเครื่องบ้านจึงยังคุ้ม** ไม่ได้เสียเปล่า — เครื่องบ้านเป็นตัวยืน
> ที่ตรงเวลา cloud เป็นตัวเสริมที่มาบ้างไม่มาบ้าง รวมแล้วได้ราว 5 จุด/วัน
> ข้อมูลซ้อนกันไม่เสียหาย (snapshot มี unique index ที่ `appid+taken_at`)
>
> **เวลาที่หน่วงมั่วมีผลกับหน้าแนวโน้มรายวัน** (ดู 3.5) เพราะการเทียบต้องใช้จุดที่
> ชั่วโมงไทยใกล้กัน ถ้าอยากให้แน่นอนต้องเพิ่ม trigger ให้ task เครื่องบ้านเป็น 4 รอบ/วัน

- เว็บ: https://dolopert.github.io/TrendGame/docs/index.html
- repo: https://github.com/Dolopert/TrendGame (public · Pages ใช้ branch `main` folder `/ (root)`)

### ข้อมูลที่สะสมไว้ (30 ส.ค. 2026)

```
Steam snapshot   15 รอบ  (27 ส.ค. 20:18 → 30 ส.ค. 05:22)
market snapshot  10 รอบ
title           727 เกม  (มีข้อมูลรีวิวแล้ว 205)
เกมที่น่าซื้อ    145 เกม
review_snapshot   0 แถว  ← ตารางใหม่ ยังว่าง ต้องรอ scan อีก 1-2 รอบ
```

**ยังไม่พอทำอะไรที่ต้องใช้เวลา** — 2.4 วันเท่านั้น และคาบ พฤหัส→อาทิตย์
ซึ่งมีผลวันในสัปดาห์ปนอยู่แยกไม่ออก

---

## 3. การตัดสินใจสำคัญ และเหตุผล

### 3.1 v1 ทำนายไม่ได้เลย — พิสูจน์ด้วยตัวเลข

แบ็คเทสอันดับของ v1 กับแค็ตตาล็อกร้านเช่าจริง (36 เกม 290 ไอดี) ได้
**Spearman = −0.07** คือไม่มีความสัมพันธ์ เกมที่ตลาดลงทุนหนักสุด (R.E.P.O. 50 ไอดี)
v1 จัดไว้อันดับ 71

สามอย่างที่ v1 เข้าใจผิด:

| v1 คิดว่า | ข้อมูลจริง |
|---|---|
| single-player ปล่อยเช่าง่ายกว่า = ข้อดี | **98% ของไอดีที่ตลาดสต็อกอยู่กับเกมที่มี Multi-player** single ล้วนได้ 1% |
| CCU/อันดับ = ดีมานด์ | วัดฐานผู้เล่น ไม่ใช่ "กลุ่มเพื่อนอยากเล่นด้วยกัน" |
| วันวางขายไม่สำคัญ | 8 ใน 12 เกมที่ตลาดสต็อกหนักสุดออกภายใน 18 เดือน |

**ทุกค่าคงที่ใน `score.py` มาจากข้อมูลจริง ไม่ได้ตั้งเอา** — ช่วงราคา, พื้น CCU 800,
เกณฑ์ evergreen 3 ใบ ล้วนมีคอมเมนต์อ้างอิงตัวเลขที่วัดได้ **อย่าแก้ค่าพวกนี้โดยไม่มี
ข้อมูลใหม่มารองรับ**

### 3.2 สิ่งที่ตั้งใจไม่ใส่เข้าสูตร

- **ชั่วโมงเล่นและคะแนนรีวิว** — เก็บแล้ว แสดงบนการ์ดแล้ว แต่ยังไม่มีผลกับคะแนน
  เพราะยังไม่รู้ว่าเกมยาวเช่าออกดีกว่าหรือแย่กว่าเกมสั้น
  (Terraria 99 ชม. ตลาดสต็อก 24 ใบ · How to Fish 6 ชม. สต็อก 15 ใบ)
- **จำนวนไอดีที่คู่แข่งถือ** — เคยคูณเข้าไป เจ้าของสั่งให้เอาออก ตัวเลขยังแสดง
  และใช้กรอง/เรียงได้ แต่ไม่บังคับเข้าคะแนน

### 3.3 ทำไมข้อมูลตลาดเก็บบน cloud ไม่ได้

หน้าร้าน 499k อยู่หลัง Cloudflare ที่กัน IP ของ datacenter — GitHub ได้ `HTTP 403`
พร้อมหน้า `<title>Just a moment...` ส่วนเน็ตบ้านผ่านปกติ

**ห้ามแก้ด้วยการหลบ challenge** (proxy, VPS, solver) นั่นคือเจาะระบบป้องกันของคนอื่น

API ทางการที่ `/docs/api` **มีของที่เราต้องการจริง แต่ยังเปิดใช้ไม่ได้**

อ่านคู่มือเต็มแล้ว (30 ส.ค.) ข้อสรุปเดิมที่ว่า "เป็น API ของรีเซลเลอร์ ใช้กับเราไม่ได้"
**ไม่ครบ** — มันมี `GET /products/{appid}/availability` ที่คืน `accounts[].busy[]`
= ช่วงเวลาที่ไอดีถูกจองจริง พร้อม `rates` รายไอดีรายจำนวนวัน

นี่คือ **ดีมานด์** ซึ่งทั้งระบบตอนนี้ยังไม่มีเลย — `stocked_total` ที่ใช้อยู่วัด *ซัพพลาย*
คือ "คู่แข่งมีของกี่ใบ" ไม่ใช่ "ของนั้นขายออกไหม" สูตรปัจจุบันถึงกับให้คะแนนสูงขึ้น
เมื่อคู่แข่งสต็อกเยอะ ทั้งที่อาจเป็นของค้างสต็อกก็ได้

**ได้คีย์ `499k_live_` มาแล้ว แต่ทุก endpoint ตอบ `403 MIN_TOPUP_REQUIRED`**
ทดสอบครบ 7 endpoint เมื่อ 30 ส.ค. — `/me`, `/products` (ทุก type), `/products/{id}`,
`/availability`, `/orders` **ตอบ 403 เหมือนกันหมด** ประตูอยู่ที่ชั้น auth ไม่ใช่รายตัว
แปลว่า**อ่านอะไรไม่ได้เลยสักอย่าง** จนกว่ายอดเติมสะสมจะครบ 2,000
(ประชดตรงที่ `/me` ซึ่งเป็น endpoint ที่ควรบอกยอดปัจจุบัน ก็โดนปิดด้วย
เลยเช็คไม่ได้ว่าตอนนี้เท่าไหร่แล้ว — บันทึกครั้งก่อนคือ 885)

> สั่งซื้อทดสอบผ่าน sandbox ไปแล้ว 1 ครั้ง (`TEST-260827-35DCF5190C`) เงื่อนไขข้อแรกผ่าน

**คำถามที่ยังตอบไม่ได้จนกว่าจะเปิดใช้ได้:** `availability` เห็นไอดีของทุกร้าน
หรือแค่ร้านที่ขายผ่าน API — ถ้าเห็นไม่ครบ utilization จะเอนเอียง
`probe_499k_api.py` เขียนรอไว้แล้ว ตอบทั้งสามข้อในคำสั่งเดียวเมื่อเติมเงินครบ

### 3.4 ปุ่มช่วงเวลาบนกราฟ และสองพาเนลบนสุด

หน้า dashboard มีกราฟบนสุดสองพาเนล ทั้งคู่มีปุ่มเลือกช่วงของตัวเอง

**พาเนล 2 — ดัชนีผู้เล่นรายเกม (กราฟเส้น · ปุ่ม `24hr / 7d / 30d / ทั้งหมด`)**
คืออันที่เจ้าของขอ ตัดช่วงใน `drawChart()` ฝั่ง JS

- **24hr กับ 1d เป็นหน้าต่างเดียวกัน จึงมีปุ่มเดียวชื่อ 24hr** แล้วเติม "ทั้งหมด"
  ไว้ดูตั้งแต่วันแรกที่เก็บ ไม่ผูกกับจำนวนวัน
- **ต้องตัดที่ `series` ของแต่ละเกม ไม่ใช่แค่กรอง stamps รวม** เพราะฐาน 100 คิดจาก
  ค่ากลางของจุดที่พล็อตจริง ถ้าไม่ตัด ฐานจะยังอิงข้อมูลนอกช่วงที่เลือก แล้วเส้นทั้งกราฟ
  จะเลื่อนขึ้นลงพร้อมกันแบบไม่มีเหตุผล
- `render.build()` ฝัง `series` เกมละ **130 จุด** (เดิม 40) = ประมาณ 30 วันที่ 4 รอบ/วัน
  ถ้าสแกนถี่ขึ้นหรือเก็บนานกว่านี้มาก ไฟล์ HTML จะบวม (ตอนนี้ 1.2 MB) ต้องกลับมาดูตรงนี้

**พาเนล 1 — ผู้เล่นรวมตามเวลา (กราฟแท่ง · ปุ่ม `1d / 7d / 30d`)**
เป็นของแถมจากรอบแรกที่เข้าใจโจทย์ผิด แต่เก็บไว้เพราะดูภาพรวมตลาดได้
คำนวณใน `render.totals_series()` ฝั่ง Python เพราะช่วง 30 วันต้องอ่าน `snapshot` ทุกแถว
**ผลรวมคิดเฉพาะ appid ที่มีครบทุกรอบในช่วงนั้น (intersection ไม่ใช่ union)** —
จำนวนเกมต่อรอบไม่เท่ากันเลย (158 → 319) ถ้าบวกดื้อ ๆ แท่งจะกระโดดเพราะ
"เก็บได้เยอะขึ้น" ไม่ใช่ "คนเล่นเยอะขึ้น" · แกนตั้งเริ่มที่ 0 เสมอ

**กฎที่ใช้ร่วมกันทั้งสองพาเนล:** ช่วงเวลานับถอยหลังจาก **จุดข้อมูลล่าสุด**
ไม่ใช่จากเวลาปัจจุบัน — ถ้าไม่ได้สแกนมาสองวัน หน้าต่าง 24hr ที่นับจาก `now`
จะว่างเปล่าทั้งที่มีข้อมูลอยู่ · ช่วงที่เก็บจริงไม่ถึงที่ขอจะขึ้นป้ายส้มติดกราฟ
บอกจำนวนชั่วโมงที่มีจริง · ปุ่มช่วงที่มีจุดข้อมูลไม่ถึงสองจุดถูก disable

> ณ 29 ส.ค. 2026 เก็บมา 37.4 ชม. → **7d / 30d / ทั้งหมด ให้ภาพเดียวกันหมด**

---

### 3.5 แนวโน้มรายวัน — ทำไมต้องเทียบชั่วโมงเดียวกัน

`trend.py` ตอบคนละคำถามกับ `score.py` **ห้ามปนกัน** — Surge ตอบว่า "ควรซื้อไหม"
ส่วน decay ตอบว่า "ควร *ไม่* ซื้อตอนนี้ไหม" (กันซื้อตอนกระแสพีคแล้วดีมานด์หาย
ก่อนคืนทุน) ยังไม่มีผลกับคะแนน เป็นข้อมูลประกอบก่อน ตามแบบเดียวกับรีวิว/ชั่วโมงเล่น

**ทางที่ลองแล้วไม่ได้ผล — อย่าลองซ้ำ**

| วิธี | ผลจริงที่วัดได้ |
|---|---|
| เทียบ CCU ดิบข้ามรอบ | ผลรวมทั้งชาร์ตแกว่ง 5.2M–9.1M ใน 2 วัน = **1.7 เท่าจากเวลาของวันล้วน ๆ** เป็นการวัดนาฬิกา |
| หารด้วย CCU รวมของตลาด | **ช่วยแค่ 8 จาก 12 เกม เฉลี่ยลด noise 4 จุด** และทำบางเกมแย่ลง (Project Zomboid CV 11.7% → 16.6%) |

ที่หารด้วยตลาดแล้วพัง เพราะ **แต่ละเกมมีโซนเวลาของตัวเอง** — Project Zomboid ฐาน
ยุโรป เอาไปหารด้วยตลาดที่เอเชียครองก็เท่ากับยัดวัฏจักรที่ไม่ใช่ของมันเข้าไป

**วิธีที่ใช้: เทียบเกมกับตัวมันเองที่ชั่วโมงไทยเดียวกันคนละวัน** ไม่ต้องสมมติว่า
ฐานผู้เล่นเหมือนใคร · ไม่บังคับชั่วโมงตายตัวเพราะรอบเก็บเลื่อน แต่ละเกมเลือกชั่วโมง
ที่ครอบคลุมวันได้มากที่สุดเอง ทน ±1 ชม. (`HOUR_TOLERANCE`)

ผล 30 ส.ค.: **115 เกมมีข้อมูล ≥3 วัน** · Shadowverse −21.5%/วัน · How to Fish −14.5%/วัน
· ELDEN RING +37.3%/วัน

> เลข How to Fish สอดคล้องกับที่เตือนไว้ตอนคะแนนพุ่ง 34.3 และหลังจากนั้น
> basis สลับเป็น `history` เอง คะแนนลงมาที่ 24.2 ตามคาด

**`review_snapshot` — ทำไมแยกตาราง** ค่ารีวิวเดี่ยว ๆ บอกได้แค่ "ดังแค่ไหน"
ความชันบอก "กำลังมาหรือกำลังไป" และ **รีวิวใหม่ต่อวันลด *ก่อน* CCU ลด**
เพราะคนซื้อวันนี้คือคนเล่นสัปดาห์หน้า = เป็นตัวชี้ *นำ* ไม่ใช่ตัวตาม
แยกจาก `snapshot` เพราะรีวิวรีเฟรชรอบละ 60 เกม ถ้ายัดรวมจะได้ค่าซ้ำเดิมเต็มไปหมด

### 3.6 ลำดับความสำคัญบนการ์ด และแผงรายละเอียด

จาก user review รอบ 30 ส.ค. — รายงานว่า `% ชอบ` กลืนกับ chip อื่น **ซึ่งจริง**
แต่เสนอให้เน้นด้วยไอคอน + เขียว/เหลือง/แดง ซึ่ง **ไม่ได้ทำ และมีเหตุผล**

1. `% ชอบ` **ไม่ได้อยู่ในสูตรคะแนนเลย** (ดู 3.2) ถ้าไปเน้นเท่ากับดันตัวเลขที่ไม่มีผล
   ต่อการตัดสินใจขึ้นเป็นพระเอก
2. แบ่งสีไม่ได้ในทางสถิติ — จาก 205 เกม **P25 75% · กลาง 87% · P75 94% และ
   60% กองอยู่ในช่วง 70-95%** ทาสามสีแล้วเกมส่วนใหญ่จะสีเดียวกันหมด

**จึงย้ายไอเดีย badge ไปใส่กับ `opportunity_score` แทน** ซึ่งเป็นตัวเลขเดียวบนการ์ด
ที่ใช้ตัดสินใจจริง พอมันเด่นชัด ความสับสนเรื่องคำศัพท์ (`คะแนนน่าซื้อ` vs `% ชอบ`)
ก็หายไปเอง ส่วน `% ชอบ` ได้แค่ไอคอน 👍 ซึ่งช่วยให้ *จำแนก* เร็วขึ้นโดยไม่เพิ่ม *น้ำหนัก*

**แผงรายละเอียด (drawer)** — กดการ์ดแล้วเปิดแผงเลื่อนจากขวา แทนที่จะเด้งไป Steam
เหตุผล: ข้อมูลลึกเคยอยู่แต่ใน `data-tip` ซึ่ง **ต้องเอาเมาส์ชี้ = มือถือเข้าไม่ถึงเลย**
(มี 5 จุดต่อการ์ด) และบันทึกถูกตัดด้วย `slice(0,3)` ที่เหลือหายเงียบ

- เลือก drawer ไม่ใช่ modal กลางจอ เพราะเห็นการ์ดอื่นค้างข้างหลัง กดดูต่อกันได้เร็ว
  และบนมือถือกลายเป็นเต็มจอเองด้วย CSS บรรทัดเดียว
- **การ์ดยังเป็น `<a>` ที่ชี้ไป Steam** ดักเฉพาะคลิกซ้ายธรรมดา —
  ctrl/cmd/shift/คลิกกลาง ยังเปิดแท็บใหม่ได้ตามพฤติกรรมลิงก์ปกติ **อย่าเปลี่ยนเป็น `<div>`**
- กราฟในแผงใช้ **CCU ดิบ** ไม่ใช่ดัชนี เพราะดูเกมเดียวไม่ได้เทียบข้ามเกม

---

### 3.7 สต็อกไอดีของร้าน อยู่คนละฐานข้อมูลกับเรดาร์ (2 ก.ย.)

`game-radar stock` เก็บ **ไอดีที่ร้านถือ · เกมในไอดี · การเช่าแต่ละครั้ง**
ที่ `data/stock.sqlite3` ซึ่งเป็นไฟล์คนละตัวกับ `radar.sqlite3` โดยเจตนา

**เหตุผลเดียวที่แยก: repo นี้ public และ `data/radar.sql` ถูก commit ทุกคืน 22:00**
ถ้าเอาอีเมล ชื่อล็อกอิน และชื่อลูกค้าไปใส่รวมในฐานเดียวกัน `dump` จะพามันขึ้น
GitHub เองโดยไม่มีใครทันสังเกต จึงไม่มีคำสั่ง `dump` ฝั่ง stock ให้เผลอใช้เลย
สำรองด้วย `stock export <โฟลเดอร์นอกโปรเจกต์>` ซึ่ง**ปฏิเสธถ้าปลายทางอยู่ใน `data/`**
และ `.gitignore` ดัก `data/stock.*` กับ `account.csv/license.csv/rental.csv` ไว้อีกชั้น

**ห้ามเก็บรหัสผ่านและ revocation code ในฐานนี้** เก็บได้แค่ `vault_ref` คือ
*ชื่อรายการ* ใน password manager — ไฟล์ SQLite ไม่ได้เข้ารหัส ใครหยิบไฟล์ไปก็อ่านได้หมด
และ revocation code มีค่าเท่ากับตัวบัญชีเอง

**ของจริงที่ได้จากตารางนี้คือดีมานด์** ซึ่งข้อ 3.3 บอกว่าทั้งระบบยังไม่มีเลย
`stocked_total` วัดซัพพลาย (คู่แข่งมีของกี่ใบ อาจเป็นของค้างสต็อก) ส่วน
`stock demand` วัดว่า *ของเราปล่อยออกกี่วันจากวันที่ปล่อยได้* แล้ววางเทียบกับคะแนน
Opportunity ที่เรดาร์ให้ไว้ — ถ้าเกมคะแนนสูงแต่ปล่อยไม่ออกซ้ำ ๆ แปลว่าสูตรใน
`score.py` ผิด ไม่ใช่ตลาดผิด นี่เป็นทางเดียวที่ตรวจสูตรได้โดยไม่ต้องรอ API 499k
ปลดล็อก (ข้อ 5.1) แต่ต้องบันทึก `--appid` ทุกครั้งที่ปล่อยเช่า ไม่งั้นได้แค่ยอดเงิน

**ยังไม่เชื่อมกับ dashboard และไม่ควรเชื่อม** `docs/index.html` ถูก push ขึ้น
GitHub Pages สาธารณะ ถ้าอยากเห็นสต็อกบนหน้าเว็บ ต้องเป็นหน้าแยกที่ไม่ถูก commit

---

### 3.8 ข้อมูลร้านเรา (หลังบ้าน 499k) — ฐานแยก ไม่ขึ้น git (5 ก.ย.)

`game-radar mine` ดึง **สถานะไอดี (ว่าง/ติดเช่า) · ยอดสรุป · ประวัติรายการเช่า/เงิน**
ของร้าน Online101Gaming จากหลังบ้าน 499k (ต้องล็อกอิน) ลง `data/own.sqlite3`
แล้วสร้าง `out/own.html` (หน้า local) — ฐานคนละตัวกับ radar **โดยเจตนาและเหตุผลเดียวกับ
ข้อ 3.7**: `radar.sql` ถูก commit ขึ้น repo สาธารณะทุกคืน ถ้ายอดเงินร้านหลุดเข้าไป
คู่แข่งอ่าน repo เห็นหมด จึงไม่มีคำสั่ง dump ฝั่งนี้ และ `.gitignore` ดัก `data/own.*`

**ดึงยังไง — ต้องผ่านเบราว์เซอร์จริง**: API หลังบ้านอยู่หลัง Cloudflare ที่ให้
**curl/httpx ได้ 502 เสมอ** (แม้ส่ง cookie ครบ 5 ก.ย. ตรวจแล้ว — non-browser TLS
โดน block) ส่วน API สาธารณะของ market.py ยังยิงตรงได้ปกติ วิธีที่ใช้: รัน fetch
ในหน้าเว็บผ่าน CDP บน **SyncProfile Brave พอร์ต 9222** (ตัวเดียวกับ iLearning cron)
ที่ login 499k ค้าง — ไม่มีการเก็บคุกกี้/รหัสลงไฟล์แต่อย่างใด

**session หมดอายุทุก ~3 วัน** (NextAuth `__Secure-next-auth.session-token`)
→ เจอหน้า login ตอนดึง จะ exit 2 พร้อมข้อความ `MINE_AUTH_EXPIRED` ให้ re-login
ใน SyncProfile Brave (Google ยัง login ค้าง ~180 วัน = 1 คลิก ไม่ต้องพิมพ์รหัสซ้ำ)
ขั้นนี้ใน `update_market.ps1` ล้มแล้วไม่หยุดทั้งงาน — ข้อมูลคนละฐานกับ radar

**ห้ามเก็บ `password`/`username` ที่ API `getrentalaccount` คืนมาเด็ดขาด**
เป็นรหัสไอดี Steam ของร้าน ตัวกรองใน `seller_fetch()` ตัดออกก่อนถึงฐานทุกครั้ง
และ schema ของ `own.sqlite3` ก็ไม่มีคอลัมน์ให้เก็บของพวกนี้อยู่แล้ว

**endpoint ที่ใช้** (verified 200 จากในเบราว์เซอร์): `storeDashboard?sold_month&sold_year`
(ยอดรวม/เดือน/วันนี้) · `getrentalaccount?page=N` (ไอดี+สถานะ) ·
`gettransactions?page=1&trans_page=N` (รายการเงิน 15/หน้า)
กับดักที่เจอ: paging ใช้ `trans_page` ไม่ใช่ `page` (ยิง `page=2` ได้หน้าเดิมซ้ำ)
และตั้งชื่อตารางว่า `txn` เพราะ `transaction` เป็นคำสงวนของ SQLite

## 4. กับดักที่เคยเสียเวลาไปแล้ว — อย่าเหยียบซ้ำ

### 4.1 ตัวคูณที่โตไม่มีขอบเขต

เกิดสองครั้ง ทั้งสองครั้งทำให้อันดับ 1 เป็นขยะ:

- เกมที่มีคนเล่น 177 คนขึ้นอันดับ 1 (โตจาก 16 คน) — แก้ด้วยพื้น `MIN_CCU_FOR_RENTAL`
- How to Fish ได้ 59.5 ขณะที่อันดับสองได้ 16 (กระโดด 145 อันดับ) — แก้ที่ต้นเหตุ
  ด้วย `_saturate()` ทั้งฝั่งอัตราโตและฝั่ง delta อันดับ

**ถ้าจะเพิ่มตัวคูณใหม่ ต้องมีเพดานเสมอ** และหลังแก้สูตรทุกครั้งให้ดู top 10
ถ้าอันดับ 1 ทิ้งห่างอันดับ 2 เกินสองเท่า ให้สงสัยไว้ก่อนว่าสูตรพัง ไม่ใช่เจอของดี

### 4.2 นับ "รอบ" ไม่ใช่ "วัน"

`ccu_history` **ยุบเป็นวันละจุด** ก่อนคำนวณอัตราโต เพราะรันสองครั้งห่างกันนาทีเดียวได้
ส่วน `ccu_series` คืนทุกจุดสำหรับกราฟ **อย่าเอาสองอันนี้สลับกัน**

### 4.3 ไฟล์ `.ps1` บน Windows เครื่องนี้

- ต้องเป็น **UTF-8 พร้อม BOM** ไม่งั้น PowerShell 5.1 อ่านไทยเป็น cp874 แล้ว parser พัง
- **ห้ามใช้ backtick ขึ้นบรรทัด** ถ้าไฟล์เป็น CRLF จะพังเงียบ ๆ ใช้ splatting `@{...}`
- Register-ScheduledTask ต้องระบุ `-TaskPath "\"` และ description เป็น ASCII

### 4.4 `getComputedStyle` โกหกเรื่อง native popup

เคยตั้ง CSS ให้ `<option>` แล้ว computed style รายงานว่าเปลี่ยนแล้ว แต่ popup จริง
ที่ Windows วาดยังขาวอยู่ **ของที่ระบบปฏิบัติการวาด ตรวจด้วย DOM API ไม่ได้ ต้องดูภาพ**
ตอนนี้ dropdown กับ tooltip เขียนเองทั้งคู่แล้ว

**แต่ยังตกไปหนึ่งอย่าง: `color-scheme:dark`** ตัวนี้ไม่ได้จัดสไตล์อะไร มันบอกเบราว์เซอร์
ว่าให้วาด native widget ทุกตัวด้วยชุดสีมืด — เป็นตัวเดียวที่แก้ "popup ขาวบนหน้ามืด"
ได้จริง จำเป็นแม้จะเขียน dropdown เองแล้ว เพราะถ้า JS ยังไม่ทันรันหรือพัง
`enhanceSelect` จะไม่ทำงาน select กลับไปเป็นของ OS ทันที แล้วได้หน้าตาแบบนี้:
ปุ่มมืด (จาก `.bar select`) + ขอบเขียว (จาก `:focus`) + รายการขาวแถบน้ำเงินของ Windows

**เวลาเห็นหน้าตาแปลก ๆ ให้ถามก่อนว่า JS รันหรือยัง** ไม่ใช่ไล่แก้ CSS

**และกับดักที่แรงกว่า: `<label>` ที่ห่อ `<select>` จะส่งต่อคลิกไปเปิด popup ของ OS**
ปุ่ม dropdown ที่เราวาดเองอยู่ข้างใน label ตัวเดียวกับ select ที่ซ่อนไว้
คลิกปุ่มทีไร Windows ก็เปิดกล่องขาวขึ้นมาซ้อนทุกที `pointer-events:none` ไม่กัน
(label สั่งเปิดตัวควบคุมตรง ๆ ไม่ได้ผ่าน pointer) `stopPropagation()` ก็ไม่กัน
(เป็น default action ไม่ใช่การ bubble) แก้ที่โครงสร้าง — `enhanceSelect` เปลี่ยน
`<label>` เป็น `<span class="fld">` ทิ้งไปเลย ไม่มี label = ไม่มีการส่งต่อ

> อาการนี้อยู่มาหลาย session โดยไม่มีใครจับได้ เพราะทุกครั้งที่ "ตรวจแล้ว"
> คือเรียก `btn.click()` จาก JS แล้วอ่าน DOM ซึ่งผิดสองชั้น: คลิกสังเคราะห์
> ไม่ทริกเกอร์ label activation เหมือนคลิกจริง และ DOM มองไม่เห็น popup ของ OS อยู่แล้ว
> **ย้ำข้อเดิม: ของที่ OS วาด ต้องดูภาพ และต้องคลิกด้วยมือจริง**

### 4.5 แทนที่ข้อความโดยไม่ assert

เกิดสองครั้งในเซสชันเดียว: เขียนสคริปต์ replace แล้วไม่ตรง มันเงียบ แล้วเดินต่อ
โดยคิดว่าแก้แล้ว **ใส่ `assert old in s` ทุกครั้ง**

### 4.6 `last_week_rank` เป็นขั้นบันได ไม่ใช่เส้นต่อเนื่อง

Steam อัปเดตชาร์ตรายสัปดาห์ **สัปดาห์ละครั้ง** แต่เราคำนวณใหม่ทุก 6 ชม.
ฐาน `weekly_rank` จึงนิ่งสนิทเป็นวัน ๆ แล้ว**กระโดดทีเดียว**ตอนชาร์ตหมุน
ไม่ใช่สูตรพัง — แต่เวลาเห็นคะแนนพุ่ง ให้เช็กก่อนว่า `last_week_rank` เพิ่งเปลี่ยนหรือเปล่า

และ Steam ส่ง **`-1`** มาเมื่อ "สัปดาห์ก่อนไม่มีอันดับ" ไม่ใช่ "อันดับที่ -1"
`if not last_week` ใน Python ไม่จับ `-1` จึงหลุดเข้าไปคิดเป็น delta ติดลบ
= กดคะแนนเกมที่เพิ่งเข้าชาร์ต แทนที่จะให้ฐาน `chart_entry`
(How to Fish ติดอันดับ 5 แต่ได้ฐานแค่ 4.45 ตลอด 27 ส.ค. เพราะเหตุนี้)
แก้แล้วใน `assess()` — normalize `<= 0` เป็น `None` ที่จุดอ่านค่าจุดเดียว
เพื่อให้ `rank_delta` และ `entered_chart` ได้ค่าถูกไปด้วย

**ค่า sentinel ของ API คนอื่น ห้ามเชื่อว่า falsy** เขียน `if (x or 0) > 0` ให้ชัด

### 4.7 เส้นโค้งบนกราฟต้องไม่สร้างข้อมูลที่ไม่มีจริง

จุดข้อมูลห่างไม่เท่ากันจริง (Actions หน่วง 43 นาที – 3.5 ชม.) เส้นโค้ง bezier ทั่วไป
จะ **overshoot คือวาดยอด/ก้นที่ไม่เคยเกิดขึ้นระหว่างจุด** ซึ่งอันตรายกับกราฟที่เอาไป
ตัดสินใจเรื่องเงิน จึงใช้ **monotone cubic (Fritsch-Carlson)** ที่การันตีว่าเส้นไม่เกิน
ค่าของสองจุดที่ขนาบอยู่

**รอบแรกเขียนตกขั้นตอนสำคัญ:** ที่จุดยอด/ก้น (ความชันสองข้างคนละเครื่องหมาย)
ต้องบังคับ tangent เป็น 0 — `if (delta[i-1] * delta[i] <= 0) m[i] = 0;`
การ clamp `α²+β² ≤ 9` อย่างเดียว **แทนไม่ได้**

วัดผลด้วยการเดินตามเส้นจริง `getPointAtLength()` 1,200 จุดต่อเส้น เทียบทีละช่วง
กับค่าของสองจุดปลาย: **ก่อนแก้เกิน 8.729px · หลังแก้ 0.000px ทั้ง 8 เส้น**

> **บทเรียน: จับได้เพราะวัด ไม่ใช่เพราะมอง** ถ้าดูด้วยตาเปล่าจะผ่านสบาย
> กราฟที่ใช้ตัดสินใจเรื่องเงินต้องมี assertion ที่รันได้ ไม่ใช่แค่ "ดูแล้วสวยดี"

### 4.8 ระวังเขียนไฟล์ชนกัน

cloud กับเครื่องบ้านเขียน `data/radar.sql` ไฟล์เดียวกัน จึงตั้งเวลาไม่ให้ชน
และทั้งคู่ `git pull --rebase` ก่อน push **ถ้าจะเพิ่มงานเก็บข้อมูลที่ไหนอีก ต้องรักษาสองข้อนี้**

ลำดับของ `update_market.ps1` **ห้ามสลับ**:
`pull → restore --force → market → scan → dash → dump → commit → push`
ถ้าข้าม `restore --force` ตอน dump จะเขียนทับด้วยฐานข้อมูลเก่าในเครื่อง ข้อมูล cloud หายทันที

**ด้านกลับที่เพิ่งโดนไปจริง ๆ:** `restore --force` เขียนทับ `radar.sqlite3` ทั้งไฟล์
**scan ที่มีอยู่แค่ในเครื่องและยังไม่ได้ `dump` + push จะหายทันที** วันที่ 28 ส.ค.
มี scan ตอน 14:22 กับ 14:28 ที่ session ก่อนรันไว้แล้วไม่ได้ push พองาน 22:00 รัน
restore ก็หายไปเลย — Steam 9 รอบเหลือ 7 · title 567 เหลือ 506 · รีวิว 140 เหลือ 0
(คะแนนไม่กระทบ เพราะทั้งสองจุดยุบเป็นจุดรายวันเดียวกับ 04:07 อยู่แล้ว
ส่วน title กับรีวิวเติมกลับเองในรอบถัดไป)

**ถ้ารัน `scan` ด้วยมือ ต้อง `dump` แล้ว commit ทันที** ไม่งั้นถือว่ายังไม่มีอยู่จริง

---

## 5. งานที่ค้างอยู่ เรียงตามผลที่จะได้

1. **ถามฝ่ายสนับสนุน 499k ว่า `availability` เห็นไอดีของทุกร้านหรือแค่ที่ขายผ่าน API**
   คำตอบข้อเดียวนี้ตัดสินว่าควรเติมเงินอีก ~1,115 บาทเพื่อปลดล็อก API หรือไม่
   ถ้าเห็นครบ จะได้ `busy[]` = ยอดจองจริง ซึ่งเป็น **ดีมานด์** ที่ระบบยังไม่มีเลย
   (ทุกอย่างตอนนี้วัดซัพพลาย) และ **ลัดงานข้อ 2 ได้ทั้งข้อ** เพราะไม่ต้องรอสะสม
   `probe_499k_api.py` เขียนรอไว้แล้ว ตอบครบสามข้อในคำสั่งเดียว — **ไม่มีค่าใช้จ่ายในการถาม**

2. **รอข้อมูลสะสม 2-3 สัปดาห์ แล้วแบ็คเทสของจริง** — market 10 รอบ Steam 15 รอบ
   ยังน้อยเกินไป คำถาม: สัญญาณจาก Steam มา *ก่อน* ที่คู่แข่งเพิ่มสต็อกหรือเปล่า
   ถ้าใช่ = ใช้ได้จริง · ถ้าไม่ = ต้องรื้อตัวชี้วัดอีกรอบ
   **รอเวลาอย่างเดียว ไม่มีทางลัด — เว้นแต่ข้อ 1 ผ่าน**

3. **ตรึงเวลา scan** ตอนนี้ Actions หน่วงมั่ว คู่จุดที่ชั่วโมงตรงกันสำหรับหน้า
   แนวโน้มรายวันเป็นความบังเอิญ ถ้าเพิ่ม trigger ให้ task เครื่องบ้านเป็น 4 รอบ/วัน
   จะได้จุดเทียบที่แน่นอน **และ** เส้นกราฟเนียนขึ้นโดยไม่ต้องปลอมข้อมูล

4. **เทียบชั่วโมงเล่นกับดีมานด์จริง** พอมี `busy[]` หรือข้อมูลสะสมพอ จะรู้ว่าควรใส่
   ชั่วโมงเล่นเข้าสูตรหรือไม่ และใส่ทางไหน (ยาว = ดี หรือ สั้น = ดี)

5. **`decay` ยังไม่เข้าสูตรคะแนน** ตั้งใจ — มี 3 วันและคาบ พฤหัส→อาทิตย์
   ต้องมีอย่างน้อย 2 สัปดาห์ให้ครบรอบวันในสัปดาห์ก่อน

6. **รีวิวยังไม่ครบ** 205/727 เกม ทยอยเต็มเองรอบละ 60 เกม ไม่ต้องทำอะไร

---

## 6. แผนที่ไฟล์

```
src/game_radar/
  steam.py    Steam API — ชาร์ต, หน้าร้าน, ค้นแท็ก co-op, appdetails
  market.py   API ร้านเช่า 499k (เก็บได้เฉพาะจากเน็ตบ้าน)
  reviews.py  รีวิว Steam → ชั่วโมงเล่น + คะแนน
  db.py       schema + migration + คำสั่งอ่านเขียน
  scan.py     การเก็บข้อมูลหนึ่งรอบ (Steam / ตลาด)
  score.py    Surge · Freshness · สต็อก → Opportunity  ← ค่าคงที่ทั้งหมดมีที่มา
  trend.py    แนวโน้มรายวัน (เทียบชั่วโมงเดียวกัน) + ความเร็วรีวิว ← คนละแกนกับ score
  render.py   สร้าง dashboard.html (กราฟ, การ์ด, ตาราง, ตัวกรอง, drawer)
  stock.py    สต็อกไอดีของร้าน — ฐานข้อมูลแยก ไม่ขึ้น git (ดู 3.7)
  mine.py     ข้อมูลร้านเราจากหลังบ้าน 499k — ฐานแยก ไม่ขึ้น git (ดู 3.8)
  cli.py      scan · market · mine · dash · top · dump · restore · run · stock

setup_scheduler.ps1   ตั้ง Task Scheduler
update_market.ps1     งานที่ task เรียก (market + scan + dash + push)
probe_499k_api.py     ตรวจ API 499k — GET ล้วน ไม่แตะ endpoint ที่ตัดเงิน
.github/workflows/daily.yml   งานบน cloud

.env            คีย์ API (gitignore ด้วย .env* — อย่าลบกฎนี้)
.env.example    แม่แบบ ตัวเดียวที่ยกเว้นด้วย !.env.example
data/radar.sql        ฐานข้อมูลในรูปข้อความ (อยู่ใน git)
data/radar.sqlite3    ฐานข้อมูลจริง (gitignore สร้างด้วย restore)
data/stock.sqlite3    สต็อกไอดีของร้าน (gitignore · ไม่มี dump · สำรองเอง)
data/own.sqlite3      ข้อมูลร้านเราจากหลังบ้าน 499k (gitignore · ไม่มี dump)
out/own.html          หน้าสรุป local ของข้อมูลร้านเรา (out/ โดน ignore)
docs/index.html       หน้าเว็บที่ deploy (generate อย่าแก้มือ)
```

ตารางใน `stock.sqlite3`: `account` (ไอดี) · `license` (เกมในไอดี) · `rental` (การเช่า)

ตารางในฐานข้อมูล: `title` · `snapshot` (CCU/อันดับรายรอบ) ·
`market_snapshot` (สต็อกตลาด) · `review_snapshot` (ประวัติจำนวนรีวิว — ใหม่)

---

## 7. คำสั่งที่ใช้บ่อย

```bash
uv run --project game-radar game-radar top -n 15        # ดูอันดับเร็ว ๆ
uv run --project game-radar game-radar restore --force  # ดึงข้อมูลจากรีโปมาเครื่อง
uv run --project game-radar game-radar dash             # สร้างหน้าเว็บใหม่

uv run --project game-radar game-radar stock list        # ไอดีไหนว่าง เลยกำหนดคืนบ้าง
uv run --project game-radar game-radar stock demand      # ปล่อยออกจริง เทียบคะแนนเรดาร์
```

ดูหน้าเว็บ: preview config ชื่อ `game-radar` (พอร์ต 5501)
